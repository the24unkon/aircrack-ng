# Aircrack-ng 

##  Usage 
 ```sh
    $ docker exec -it app sh
    // Inside container shell
     $ docker exec -it app sh
    // Inside container shell
    $ docker exec -it app sh
    // Inside container shell
    $ docker exec -it app sh
    // Inside container shell
   
    
